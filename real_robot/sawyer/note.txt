
```

cd ~/ros_ws/src
git clone https://github.com/RethinkRobotics/intera_sdk.git
cd ~/ros_ws
catkin_make


```

```
source ~/ros_ws/devel/setup.bash

# set robot IP
./intera.sh

export ROS_MASTER_URI=http://<robot-ip>:11311
export ROS_IP=<your-laptop-ip>
```


I should see

```

rostopic list

# /robot/joint_states
# /robot/limb/right/joint_command
# /robot/head/...


```

run

```
rosrun my_package test_robot.py

```


For moveit

```
roslaunch sawyer_moveit_config sawyer_moveit.launch

```


docker run --rm -it --net=host \
     -e ROS_MASTER_URI=http://<ROBOT_IP>:11311 \
     -e ROS_IP=<YOUR_LAPTOP_IP> \
     -v $(pwd)/intera_ws:/home/developer/intera_ws \
     sawyer:kinetic /bin/bash

launch docker with -e DISPLAY=$DISPLAY -v /tmp/.X11-unix:/tmp/.X11-unix and (on Linux) xhost +local:root (be careful with security).